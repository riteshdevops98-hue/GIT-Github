Git File Lifecycle 

Full Git Cycle

Initialize repo

git init


Add file (Untracked → Staged)

git add <file>


Commit (Staged → Local repo)

git commit -m "message"


Connect remote

git remote add origin <repo-url>


Push (Local → Remote)

git push -u origin main

⚡ Flow
init → add → commit → push
(Local repo → Remote repo)
